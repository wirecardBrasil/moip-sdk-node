var chai = require('chai');
var should = chai.should();

var Moip = require('../index').Moip;

var moip = new Moip({
  'user': "test"
});

describe('Moip Test', function() {

    it('basic test', function(done) {
        done();
    });
});
